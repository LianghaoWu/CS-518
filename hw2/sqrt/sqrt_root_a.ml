(*
    Not quite sure about the function coverage
    If I want to test the sqrt_root I have to call sqrt_root, then I satisified function coverage according to the sildes, because I have sqrt_root at least once.
 *)